/**
 * Single source of truth for the API version reported by `/health`.
 * Kept in sync with the `version` field of package.json.
 */
export const SERVICE_VERSION = '1.0.0';
export const SERVICE_NAME = 'linguaverse-api';