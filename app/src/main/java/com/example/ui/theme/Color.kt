package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ---------------------------------------------------------------------------
// LinguaVerse Brand Palette
// ---------------------------------------------------------------------------

// Primary — Midnight Indigo (trust, intelligence, calm learning)
val PrimaryIndigo = Color(0xFF4F46E5)
val PrimaryIndigoLight = Color(0xFF818CF8)
val PrimaryIndigoDark = Color(0xFFA5B4FC)

// Secondary — Aurora Emerald (growth, progress, nature)
val SecondaryEmerald = Color(0xFF059669)
val SecondaryEmeraldLight = Color(0xFF34D399)
val SecondaryEmeraldDark = Color(0xFF6EE7B7)

// Tertiary — Warm Amber (rewards, energy, achievement)
val TertiaryAmber = Color(0xFFD97706)
val TertiaryAmberLight = Color(0xFFFBBF24)
val TertiaryAmberDark = Color(0xFFFCD34D)

// Error — Signal Red (mistakes, corrections)
val ErrorRed = Color(0xFFDC2626)
val ErrorRedLight = Color(0xFFFCA5A5)

// ---------------------------------------------------------------------------
// Surface System (Light)
// ---------------------------------------------------------------------------
val SurfaceLight = Color(0xFFF7F8FC)
val SurfaceContainerLowestLight = Color(0xFFFFFFFF)
val SurfaceContainerLight = Color(0xFFFFFFFF)
val SurfaceContainerHighLight = Color(0xFFF0F1F8)
val SurfaceContainerHighestLight = Color(0xFFE8EAF2)

// ---------------------------------------------------------------------------
// Surface System (Dark)
// ---------------------------------------------------------------------------
val SurfaceDark = Color(0xFF0B1020)
val SurfaceContainerLowestDark = Color(0xFF060A16)
val SurfaceContainerDark = Color(0xFF151C30)
val SurfaceContainerHighDark = Color(0xFF202A43)
val SurfaceContainerHighestDark = Color(0xFF2B3756)

// ---------------------------------------------------------------------------
// Scrim & Shadow
// ---------------------------------------------------------------------------
val ScrimLight = Color(0x66000000)   // 40% black
val ScrimDark = Color(0x99000000)    // 60% black

// ---------------------------------------------------------------------------
// Utility
// ---------------------------------------------------------------------------
val White = Color.White
val Black = Color.Black
val Transparent = Color.Transparent
