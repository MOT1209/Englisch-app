# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# The locale is pinned so annotation processors emit locale-neutral source. Room's
# KSP generator formats numbers with the JVM's FORMAT locale category; on Windows
# that category comes from the OS "regional format" setting, so on an Arabic-locale
# machine it writes Arabic-Indic digits into generated Kotlin
# (`RoomOpenDelegate(١, ...)`) and compilation fails. Note that user.language alone
# is NOT enough — the .format properties are what String.format actually reads.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8 -Duser.language=en -Duser.country=US -Duser.language.format=en -Duser.country.format=US
# KSP and the Kotlin compiler run in their own worker JVMs, which do not inherit
# org.gradle.jvmargs, so the locale must be pinned for them separately.
kotlin.daemon.jvmargs=-Duser.language=en -Duser.country=US -Duser.language.format=en -Duser.country.format=US
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.caching=true
org.gradle.configuration-cache=true
# Set the maximum number of workers to 4 to avoid overloading the machine.
org.gradle.workers.max=4
# Set the Kotlin compiler execution strategy to in-process to avoid "Could not
# connect to Kotlin compile daemon" error.
kotlin.compiler.execution.strategy=in-process
# Allow the google-services plugin to work even if the google-services.json is not present.
googleServices.missing.passthrough=true
